<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keymanager.monitoring.dao.ApplicationMarketDao">
    <sql id="colID">
      am.fUuid AS 'uuid',
      am.fMarketName AS 'marketName',
      am.fMarketPackageName AS 'marketPackageName',
      am.fTmpPath AS 'tmpPath',
      am.fApkPath AS 'apkPath',
      am.fDataDBPath AS 'dataDBPath',
      am.fStorageDBPath AS 'storageDBPath',
      am.fFileType AS 'fileType'
    </sql>
    <select id="getmarketInfo" resultType="com.keymanager.monitoring.entity.ApplicationMarket">
      SELECT
        <include refid="colID"/>
      FROM
        t_application_market_info am
    </select>

    <select id="searchpplicationMarket" resultType="com.keymanager.monitoring.entity.ApplicationMarket">
      SELECT
        <include refid="colID"/>
       FROM
        t_application_market_info am
      WHERE
        1=1
        <if test="applicationMarketCriteria.marketName != null and applicationMarketCriteria.marketName !=''">
            and am.fMarketName like '%${applicationMarketCriteria.marketName}%'
        </if>
    </select>

</mapper>